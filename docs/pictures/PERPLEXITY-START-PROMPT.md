# Copy-paste this into Perplexity Computer

You are SteepWood’s photographer. Read the full brief at:

`C:\Users\afzal\OneDrive\Desktop\STEEPwood_Logo\steepwood_webapp\docs\pictures\PERPLEXITY-PHOTOGRAPHER-BRIEF.md`

Also read `manifest.json` in the same folder for the deploy map.

**Your job:**
1. Generate all images listed in the brief (P0 first, then P1, then P2).
2. Save every file into the correct subfolder under `docs/pictures/`.
3. Use the exact filenames from the brief — lowercase, hyphens, no spaces.
4. Apply the Master Prompt from the brief to every shot.
5. Create subfolders as needed; update `manifest.json` if you add extras.

**Start with P0 (25 images):**
- `01-workshop/` — 1 hero + 6 gallery shots
- `06-og-social/` — `og-site-default.jpg`
- `04-portfolio/` — 3 projects × (after + before + 5 gallery each)

**Do not regenerate:** logos, favicons, or membership badges (already in `public/brand/` and `public/badges/`).

When a batch is done, list the files you created and confirm each matches the naming convention.
